https://codeforces.com/contest/2148/problem/E

k multiset
 
```
map_all;
for(auto key, value : map_ij){
    k*map_ij[key]==map_all[key];
}
```

$$
\begin{align}
&\exist n>=0, map_{all}[key] - map[key] -n =(k-1)(map[key]+n) \\
&\exist n>=0, map_{all}[key] - k\cdot map[key] =k\cdot n \\
& map_{all}[key] - k\cdot map[key] \equiv 0(\mod k) \\
&- k \mod k
\end{align} 
$$

can put, but can't take. 
1. count number, must be k times. for every element `e`, `num_e = map[e]`
2. count l, r, number in them must smaller than `num_e/k`. if one exceed, then we can move l try to decrease. 

2
3 3 3 3 2 2 2 2
3 , 1
3 3 , 2
  3 3, 2
    3 3 2 2, 2+3+4=9
          2 2, 2
            2 2, 2

    