todo: 
- BFS, find the point of node with no in-edges. copy edges matrix to change in progress. 
add a method to sort as some creterion, like degree, then we need to mark degree.
- if you have need of store issentially different things but with some value. then use pointer. because searching is a visit_node. 
- DFS
- topol sort. 
- Dijkstra.
- connectted subgraph.
implement them in or out of graph? without need, we don't know.

- graphviz.

